"""
NumPy Tasks — Навчальний скрипт для практики NumPy.

Завдання:
1. Створити та маніпулювати масивами
2. Статистичні розрахунки
3. Маскування та фільтрація
4. Broadcasting
5. Матричні операції
6. Random генерація
7. Reshape та трансформації
8. Векторизація обчислень
9. Робота з пропусками
10. Агрегація даних

Кожне завдання має коментар з описом.
Заповніть код після коментаря # TODO:
"""

import numpy as np

# =========================================
# Завдання 1: Створення масивів
# =========================================
# Створіть одновимірний масив з 15 елементів від 10 до 24
arr1 = np.arange(10, 25)

# =========================================
# Завдання 2: Матриці та reshape
# =========================================
# Створіть матрицю 5x5 з випадковими цілими числами від 0 до 100
matrix = np.random.randint(0, 101, size=(5, 5))

# =========================================
# Завдання 3: Статистика
# =========================================
# Для матриці 4x4 знайдіть середнє, медіану, стандартне відхилення
# для кожного рядка та кожного стовпця
data = np.random.randint(1, 100, size=(4, 4))
# обчисліть статистики
row_means = np.mean(data, axis=1)
col_means = np.mean(data, axis=0)
row_std = np.std(data, axis=1)
col_median = np.median(data, axis=0)

# =========================================
# Завдання 4: Маскування
# =========================================
# Створіть масив з 50 випадкових чисел. Знайдіть:
# - кількість елементів > 0.5
# - індекси елементів > 0.8
# - середнє значення елементів у діапазоні [0.3, 0.7]
rand_arr = np.random.random(50)
# TODO:
count_gt_05 = np.sum(rand_arr > 0.5)
indices_gt_08 = np.where(rand_arr > 0.8)[0]
mean_range = np.mean(rand_arr[(rand_arr >= 0.3) & (rand_arr <= 0.7)])

# =========================================
# Завдання 5: Broadcasting
# =========================================
# Маємо матрицю 3x4. Додайте до кожного рядка вектор [1, 2, 3, 4]
# без використання циклів
mat = np.random.randint(1, 10, size=(3, 4))
vec = np.array([1, 2, 3, 4])
# TODO:
result = mat + vec  # broadcasting: додаємо вектор до кожного рядка

# =========================================
# Завдання 6: Матричне множення
# =========================================
# Створіть дві матриці 3x3 та перемножте їх
A = np.random.randint(1, 10, size=(3, 3))
B = np.random.randint(1, 10, size=(3, 3))
# TODO:
product = A @ B  # матричне множення (еквівалент np.dot(A, B))

# =========================================
# Завдання 7: Робота з пропусками
# =========================================
# Маємо масив з NaN значеннями. Замініть NaN на середнє значення масиву
arr_with_nan = np.array([1.0, 2.0, np.nan, 4.0, np.nan, 6.0, 7.0, np.nan])
# TODO:
mean_val = np.nanmean(arr_with_nan)          # середнє без NaN: (1+2+4+6+7)/5 = 4.0
filled_arr = np.where(np.isnan(arr_with_nan), mean_val, arr_with_nan)

# =========================================
# Завдання 8: Векторизація
# =========================================
# Обчисліть суму квадратів для кожного рядка матриці 1000x100
# ВИКОРИСТОВУЙТЕ векторизацію (жодних циклів)
big_matrix = np.random.randint(1, 100, size=(1000, 100))
# TODO:
sum_of_squares = np.sum(big_matrix ** 2, axis=1)  # векторизовано! без циклів

# =========================================
# Завдання 9: Умовна заміна
# =========================================
# У масиві з 100 випадкових чисел замініть всі від'ємні на 0,
# всі > 0.8 на 1, решту на 0.5
cond_arr = np.random.randn(100)  # нормальний розподіл (будуть від'ємні)
# TODO:
transformed = np.select(
    [cond_arr < 0, cond_arr > 0.8],  # умови
    [0, 1],                           # значення для умов
    default=0.5                       # решта
)

# =========================================
# Завдання 10: Агрегація з групуванням
# =========================================
# Маємо 100 точок з координатами x, y та мітками груп (0-4).
# Знайдіть центроїд (середнє x, середнє y) для кожної групи
x = np.random.randn(100)
y = np.random.randn(100)
groups = np.random.randint(0, 5, size=100)
# TODO:
centroids = np.array([
    [np.mean(x[groups == g]), np.mean(y[groups == g])]
    for g in range(5)
])  # shape (5, 2) — центроїд для кожної групи


if __name__ == "__main__":
    print("=" * 50)
    print("NumPy Tasks")
    print("=" * 50)
    print("Запустіть кожне завдання окремо або весь скрипт.")
    print("Після виконання всіх завдань запустіть: python main.py grade")
